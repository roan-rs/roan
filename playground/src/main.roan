use { println } from "std::debug"
use { base64_url, Base64Encoder, BASE64_ALPHABET } from "std::base64"

pub fn main() -> int {
    let x = Base64Encoder::new(BASE64_ALPHABET, null);

    let url = x.encode("https://roan-lang.org");
    println("Base64 URL: {}", url);

    return 0;
}

main();
