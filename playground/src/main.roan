use { add } from "./add.roan";

fn println(...args: string) -> int {
    return 0;
}

export fn main() -> int {
    let vec = [1, 2, 3, 4, 5];
    let sum = add(10, 2.3);

    if 2 + 2 == 8  {
        println("Success!");
    } else if sum > 11.7 {
        println("Greater than expected!");
    } else {
        println("Failure!");
    }

    return 0;
}

main();