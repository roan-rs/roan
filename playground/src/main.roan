use { println } from "std::io";

pub fn main() -> int {
    let test = null;

    if test {
        println("1 + 1 is 2!");
    } else {
        println("1 + 1 is not 2!");
    }

    return 0;
}

main();
