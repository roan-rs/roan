use { println, eprintln } from "std::io";

export fn add(a: float, b: float) -> float {
    return a + b;
}

export fn main() -> int {
    let i = 3.14;
    let j = true;

    if j {
        i = add(i, 2.0);
    } else {
        eprintln("Goodbye, world!");
    }

    return 0;
}
