/// Struct for string manipulation
pub struct String {
    value: string,
    xd: int
}

impl String {
    pub fn new() -> string {
        return "";
    }

    // TODO: refactor this to use match in future
    pub fn to_bool(s: string) -> bool? {
        if s == "true" {
            return true;
        } else if s == "false" {
        }
    }
}