use { println } from "std::io";

pub fn test(arr: string[]) -> string[] {
    return arr
}

pub fn main() -> int {
    let name = "Alice";
    println("name is a {}", type_of(name));

    let fail = test(["Alice", 32]);

    return 0;
}

main();
