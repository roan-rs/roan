use {println,format} from "std::io";

export fn main() -> int {
    let x = [1, 2, ...[3, 4, 5]];

    let msg = format("{} {}", x, 5);

    println("Hello, world! {}", msg);

    return 0;
}

main();