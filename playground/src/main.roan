use { add } from "./add.roan";

fn println(...args: string) -> int {
    return 0;
}

export fn main() -> int {
    let i = 3.14;

    let sum = add(i, 2.71 + 1);

    if sum == 11.7 {
        println("Success!");
    } else if sum > 11.7 {
        println("Greater than expected!");
    } else {
        println("Failure!");
    }

    return 0;
}

main();