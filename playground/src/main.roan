use { String } from "std::string"
use { println } from "std::debug"

fn print(text: string, ...args: anytype) -> void {
    println(text, ...args);
}

pub fn main() -> int {
    let c = 'a';

    return 0;
}

main();
