use {eprintln,format} from "std::io";

pub struct Person {
    name: string,
    age: int,
}

trait PersonTrait {
    fn new(name: string, age: int) -> Self;
    fn get_name(self) -> string;
    fn get_age(self) -> int;
}

pub fn main() -> int {
    return 0;
}

main();